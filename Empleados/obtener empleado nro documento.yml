info:
  name: obtener empleado nro documento
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/empleados/nro-documento/71497766"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
