info:
  name: valores normales
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/triajes/valores-normales"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
