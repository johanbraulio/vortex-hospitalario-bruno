info:
  name: crear usuario
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/usuarios"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "username": "test",
        "password": "test123",
        "empleadoId": 4676,
        "rolIds": [1,2]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
