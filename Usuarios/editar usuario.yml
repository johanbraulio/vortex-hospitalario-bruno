info:
  name: editar usuario
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/usuarios/4"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "username": "actualizacion",
        "password": "actualizacion",
        "activo": true,
        "rolIds": [1,2]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
