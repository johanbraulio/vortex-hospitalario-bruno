info:
  name: obtener usuario nroDocumento
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/usuarios/nro-documento/71497766"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
